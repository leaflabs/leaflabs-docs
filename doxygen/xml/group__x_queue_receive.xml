<?xml version='1.0' encoding='UTF-8' standalone='no'?>
<doxygen xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="compound.xsd" version="1.7.6.1">
  <compounddef id="group__x_queue_receive" kind="group">
    <compoundname>xQueueReceive</compoundname>
    <title>xQueueReceive</title>
    <briefdescription>
<para>queue. </para>    </briefdescription>
    <detaileddescription>
<para>h <preformatted>
 portBASE_TYPE xQueuePeek(
							 xQueueHandle xQueue,
							 void *pvBuffer,
							 portTickType xTicksToWait
						 );</preformatted></para><para>This is a macro that calls the xQueueGenericReceive() function.</para><para>Receive an item from a queue without removing the item from the queue. The item is received by copy so a buffer of adequate size must be provided. The number of bytes copied into the buffer was defined when the queue was created.</para><para>Successfully received items remain on the queue so will be returned again by the next call, or a call to xQueueReceive().</para><para>This macro must not be used in an interrupt service routine.</para><para><parameterlist kind="param"><parameteritem>
<parameternamelist>
<parametername>pxQueue</parametername>
</parameternamelist>
<parameterdescription>
<para>The handle to the queue from which the item is to be received.</para></parameterdescription>
</parameteritem>
<parameteritem>
<parameternamelist>
<parametername>pvBuffer</parametername>
</parameternamelist>
<parameterdescription>
<para>Pointer to the buffer into which the received item will be copied.</para></parameterdescription>
</parameteritem>
<parameteritem>
<parameternamelist>
<parametername>xTicksToWait</parametername>
</parameternamelist>
<parameterdescription>
<para>The maximum amount of time the task should block waiting for an item to receive should the queue be empty at the time of the call. The time is defined in tick periods so the constant portTICK_RATE_MS should be used to convert to real time if this is required. xQueuePeek() will return immediately if xTicksToWait is 0 and the queue is empty.</para></parameterdescription>
</parameteritem>
</parameterlist>
<simplesect kind="return"><para>pdTRUE if an item was successfully received from the queue, otherwise pdFALSE.</para></simplesect>
Example usage: <preformatted>
 struct AMessage
 {
	char ucMessageID;
	char ucData[ 20 ];
 } xMessage;</preformatted></para><para><preformatted> xQueueHandle xQueue;</preformatted></para><para><preformatted>Task to create a queue and post a value.
 void vATask( void *pvParameters )
 {
 struct AMessage *pxMessage;</preformatted></para><para><preformatted>Create a queue capable of containing 10 pointers to AMessage structures.
These should be passed by pointer as they contain a lot of data.
	xQueue = xQueueCreate( 10, sizeof( struct AMessage * ) );
	if( xQueue == 0 )
	{
Failed to create the queue.
	}</preformatted></para><para><preformatted>...</preformatted></para><para><preformatted>Send a pointer to a struct AMessage object.  Don&apos;t block if the
queue is already full.
	pxMessage = &amp; xMessage;
	xQueueSend( xQueue, ( void * ) &amp;pxMessage, ( portTickType ) 0 );</preformatted></para><para><preformatted>... Rest of task code.
 }</preformatted></para><para><preformatted>Task to peek the data from the queue.
 void vADifferentTask( void *pvParameters )
 {
 struct AMessage *pxRxedMessage;</preformatted></para><para><preformatted>	if( xQueue != 0 )
	{
Peek a message on the created queue.  Block for 10 ticks if a
message is not immediately available.
		if( xQueuePeek( xQueue, &amp;( pxRxedMessage ), ( portTickType ) 10 ) )
		{
pcRxedMessage now points to the struct AMessage variable posted
by vATask, but the item still remains on the queue.
		}
	}</preformatted></para><para><preformatted>... Rest of task code.
 }
 </preformatted></para><para>h <preformatted>
 portBASE_TYPE xQueueReceive(
								 xQueueHandle xQueue,
								 void *pvBuffer,
								 portTickType xTicksToWait
							);</preformatted></para><para>This is a macro that calls the xQueueGenericReceive() function.</para><para>Receive an item from a queue. The item is received by copy so a buffer of adequate size must be provided. The number of bytes copied into the buffer was defined when the queue was created.</para><para>Successfully received items are removed from the queue.</para><para>This function must not be used in an interrupt service routine. See xQueueReceiveFromISR for an alternative that can.</para><para><parameterlist kind="param"><parameteritem>
<parameternamelist>
<parametername>pxQueue</parametername>
</parameternamelist>
<parameterdescription>
<para>The handle to the queue from which the item is to be received.</para></parameterdescription>
</parameteritem>
<parameteritem>
<parameternamelist>
<parametername>pvBuffer</parametername>
</parameternamelist>
<parameterdescription>
<para>Pointer to the buffer into which the received item will be copied.</para></parameterdescription>
</parameteritem>
<parameteritem>
<parameternamelist>
<parametername>xTicksToWait</parametername>
</parameternamelist>
<parameterdescription>
<para>The maximum amount of time the task should block waiting for an item to receive should the queue be empty at the time of the call. xQueueReceive() will return immediately if xTicksToWait is zero and the queue is empty. The time is defined in tick periods so the constant portTICK_RATE_MS should be used to convert to real time if this is required.</para></parameterdescription>
</parameteritem>
</parameterlist>
<simplesect kind="return"><para>pdTRUE if an item was successfully received from the queue, otherwise pdFALSE.</para></simplesect>
Example usage: <preformatted>
 struct AMessage
 {
	char ucMessageID;
	char ucData[ 20 ];
 } xMessage;</preformatted></para><para><preformatted> xQueueHandle xQueue;</preformatted></para><para><preformatted>Task to create a queue and post a value.
 void vATask( void *pvParameters )
 {
 struct AMessage *pxMessage;</preformatted></para><para><preformatted>Create a queue capable of containing 10 pointers to AMessage structures.
These should be passed by pointer as they contain a lot of data.
	xQueue = xQueueCreate( 10, sizeof( struct AMessage * ) );
	if( xQueue == 0 )
	{
Failed to create the queue.
	}</preformatted></para><para><preformatted>...</preformatted></para><para><preformatted>Send a pointer to a struct AMessage object.  Don&apos;t block if the
queue is already full.
	pxMessage = &amp; xMessage;
	xQueueSend( xQueue, ( void * ) &amp;pxMessage, ( portTickType ) 0 );</preformatted></para><para><preformatted>... Rest of task code.
 }</preformatted></para><para><preformatted>Task to receive from the queue.
 void vADifferentTask( void *pvParameters )
 {
 struct AMessage *pxRxedMessage;</preformatted></para><para><preformatted>	if( xQueue != 0 )
	{
Receive a message on the created queue.  Block for 10 ticks if a
message is not immediately available.
		if( xQueueReceive( xQueue, &amp;( pxRxedMessage ), ( portTickType ) 10 ) )
		{
pcRxedMessage now points to the struct AMessage variable posted
by vATask.
		}
	}</preformatted></para><para><preformatted>... Rest of task code.
 }
 </preformatted></para><para>h <preformatted>
 portBASE_TYPE xQueueGenericReceive(
									   xQueueHandle	xQueue,
									   void	*pvBuffer,
									   portTickType	xTicksToWait
									   portBASE_TYPE	xJustPeek
									);</preformatted></para><para>It is preferred that the macro xQueueReceive() be used rather than calling this function directly.</para><para>Receive an item from a queue. The item is received by copy so a buffer of adequate size must be provided. The number of bytes copied into the buffer was defined when the queue was created.</para><para>This function must not be used in an interrupt service routine. See xQueueReceiveFromISR for an alternative that can.</para><para><parameterlist kind="param"><parameteritem>
<parameternamelist>
<parametername>pxQueue</parametername>
</parameternamelist>
<parameterdescription>
<para>The handle to the queue from which the item is to be received.</para></parameterdescription>
</parameteritem>
<parameteritem>
<parameternamelist>
<parametername>pvBuffer</parametername>
</parameternamelist>
<parameterdescription>
<para>Pointer to the buffer into which the received item will be copied.</para></parameterdescription>
</parameteritem>
<parameteritem>
<parameternamelist>
<parametername>xTicksToWait</parametername>
</parameternamelist>
<parameterdescription>
<para>The maximum amount of time the task should block waiting for an item to receive should the queue be empty at the time of the call. The time is defined in tick periods so the constant portTICK_RATE_MS should be used to convert to real time if this is required. xQueueGenericReceive() will return immediately if the queue is empty and xTicksToWait is 0.</para></parameterdescription>
</parameteritem>
<parameteritem>
<parameternamelist>
<parametername>xJustPeek</parametername>
</parameternamelist>
<parameterdescription>
<para>When set to true, the item received from the queue is not actually removed from the queue - meaning a subsequent call to xQueueReceive() will return the same item. When set to false, the item being received from the queue is also removed from the queue.</para></parameterdescription>
</parameteritem>
</parameterlist>
<simplesect kind="return"><para>pdTRUE if an item was successfully received from the queue, otherwise pdFALSE.</para></simplesect>
Example usage: <preformatted>
 struct AMessage
 {
	char ucMessageID;
	char ucData[ 20 ];
 } xMessage;</preformatted></para><para><preformatted> xQueueHandle xQueue;</preformatted></para><para><preformatted>Task to create a queue and post a value.
 void vATask( void *pvParameters )
 {
 struct AMessage *pxMessage;</preformatted></para><para><preformatted>Create a queue capable of containing 10 pointers to AMessage structures.
These should be passed by pointer as they contain a lot of data.
	xQueue = xQueueCreate( 10, sizeof( struct AMessage * ) );
	if( xQueue == 0 )
	{
Failed to create the queue.
	}</preformatted></para><para><preformatted>...</preformatted></para><para><preformatted>Send a pointer to a struct AMessage object.  Don&apos;t block if the
queue is already full.
	pxMessage = &amp; xMessage;
	xQueueSend( xQueue, ( void * ) &amp;pxMessage, ( portTickType ) 0 );</preformatted></para><para><preformatted>... Rest of task code.
 }</preformatted></para><para><preformatted>Task to receive from the queue.
 void vADifferentTask( void *pvParameters )
 {
 struct AMessage *pxRxedMessage;</preformatted></para><para><preformatted>	if( xQueue != 0 )
	{
Receive a message on the created queue.  Block for 10 ticks if a
message is not immediately available.
		if( xQueueGenericReceive( xQueue, &amp;( pxRxedMessage ), ( portTickType ) 10 ) )
		{
pcRxedMessage now points to the struct AMessage variable posted
by vATask.
		}
	}</preformatted></para><para><preformatted>... Rest of task code.
 }
 </preformatted> </para>    </detaileddescription>
  </compounddef>
</doxygen>
