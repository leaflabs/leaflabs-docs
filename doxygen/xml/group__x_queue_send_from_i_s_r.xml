<?xml version='1.0' encoding='UTF-8' standalone='no'?>
<doxygen xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="compound.xsd" version="1.7.6.1">
  <compounddef id="group__x_queue_send_from_i_s_r" kind="group">
    <compoundname>xQueueSendFromISR</compoundname>
    <title>xQueueSendFromISR</title>
    <briefdescription>
<para>queue. </para>    </briefdescription>
    <detaileddescription>
<para>h <preformatted>
 portBASE_TYPE xQueueSendToFrontFromISR(
										 xQueueHandle pxQueue,
										 const void *pvItemToQueue,
										 portBASE_TYPE *pxHigherPriorityTaskWoken
									  );
 </preformatted></para><para>This is a macro that calls xQueueGenericSendFromISR().</para><para>Post an item to the front of a queue. It is safe to use this macro from within an interrupt service routine.</para><para>Items are queued by copy not reference so it is preferable to only queue small items, especially when called from an ISR. In most cases it would be preferable to store a pointer to the item being queued.</para><para><parameterlist kind="param"><parameteritem>
<parameternamelist>
<parametername>xQueue</parametername>
</parameternamelist>
<parameterdescription>
<para>The handle to the queue on which the item is to be posted.</para></parameterdescription>
</parameteritem>
<parameteritem>
<parameternamelist>
<parametername>pvItemToQueue</parametername>
</parameternamelist>
<parameterdescription>
<para>A pointer to the item that is to be placed on the queue. The size of the items the queue will hold was defined when the queue was created, so this many bytes will be copied from pvItemToQueue into the queue storage area.</para></parameterdescription>
</parameteritem>
<parameteritem>
<parameternamelist>
<parametername>pxHigherPriorityTaskWoken</parametername>
</parameternamelist>
<parameterdescription>
<para>xQueueSendToFrontFromISR() will set *pxHigherPriorityTaskWoken to pdTRUE if sending to the queue caused a task to unblock, and the unblocked task has a priority higher than the currently running task. If xQueueSendToFromFromISR() sets this value to pdTRUE then a context switch should be requested before the interrupt is exited.</para></parameterdescription>
</parameteritem>
</parameterlist>
<simplesect kind="return"><para>pdTRUE if the data was successfully sent to the queue, otherwise errQUEUE_FULL.</para></simplesect>
Example usage for buffered IO (where the ISR can obtain more than one value per call): <preformatted>
 void vBufferISR( void )
 {
 char cIn;
 portBASE_TYPE xHigherPrioritTaskWoken;</preformatted></para><para><preformatted>We have not woken a task at the start of the ISR.
	xHigherPriorityTaskWoken = pdFALSE;</preformatted></para><para><preformatted>Loop until the buffer is empty.
	do
	{
Obtain a byte from the buffer.
		cIn = portINPUT_BYTE( RX_REGISTER_ADDRESS );</preformatted></para><para><preformatted>Post the byte.
		xQueueSendToFrontFromISR( xRxQueue, &amp;cIn, &amp;xHigherPriorityTaskWoken );</preformatted></para><para><preformatted>	} while( portINPUT_BYTE( BUFFER_COUNT ) );</preformatted></para><para><preformatted>Now the buffer is empty we can switch context if necessary.
	if( xHigherPriorityTaskWoken )
	{
		taskYIELD ();
	}
 }
 </preformatted></para><para>h <preformatted>
 portBASE_TYPE xQueueSendToBackFromISR(
										 xQueueHandle pxQueue,
										 const void *pvItemToQueue,
										 portBASE_TYPE *pxHigherPriorityTaskWoken
									  );
 </preformatted></para><para>This is a macro that calls xQueueGenericSendFromISR().</para><para>Post an item to the back of a queue. It is safe to use this macro from within an interrupt service routine.</para><para>Items are queued by copy not reference so it is preferable to only queue small items, especially when called from an ISR. In most cases it would be preferable to store a pointer to the item being queued.</para><para><parameterlist kind="param"><parameteritem>
<parameternamelist>
<parametername>xQueue</parametername>
</parameternamelist>
<parameterdescription>
<para>The handle to the queue on which the item is to be posted.</para></parameterdescription>
</parameteritem>
<parameteritem>
<parameternamelist>
<parametername>pvItemToQueue</parametername>
</parameternamelist>
<parameterdescription>
<para>A pointer to the item that is to be placed on the queue. The size of the items the queue will hold was defined when the queue was created, so this many bytes will be copied from pvItemToQueue into the queue storage area.</para></parameterdescription>
</parameteritem>
<parameteritem>
<parameternamelist>
<parametername>pxHigherPriorityTaskWoken</parametername>
</parameternamelist>
<parameterdescription>
<para>xQueueSendToBackFromISR() will set *pxHigherPriorityTaskWoken to pdTRUE if sending to the queue caused a task to unblock, and the unblocked task has a priority higher than the currently running task. If xQueueSendToBackFromISR() sets this value to pdTRUE then a context switch should be requested before the interrupt is exited.</para></parameterdescription>
</parameteritem>
</parameterlist>
<simplesect kind="return"><para>pdTRUE if the data was successfully sent to the queue, otherwise errQUEUE_FULL.</para></simplesect>
Example usage for buffered IO (where the ISR can obtain more than one value per call): <preformatted>
 void vBufferISR( void )
 {
 char cIn;
 portBASE_TYPE xHigherPriorityTaskWoken;</preformatted></para><para><preformatted>We have not woken a task at the start of the ISR.
	xHigherPriorityTaskWoken = pdFALSE;</preformatted></para><para><preformatted>Loop until the buffer is empty.
	do
	{
Obtain a byte from the buffer.
		cIn = portINPUT_BYTE( RX_REGISTER_ADDRESS );</preformatted></para><para><preformatted>Post the byte.
		xQueueSendToBackFromISR( xRxQueue, &amp;cIn, &amp;xHigherPriorityTaskWoken );</preformatted></para><para><preformatted>	} while( portINPUT_BYTE( BUFFER_COUNT ) );</preformatted></para><para><preformatted>Now the buffer is empty we can switch context if necessary.
	if( xHigherPriorityTaskWoken )
	{
		taskYIELD ();
	}
 }
 </preformatted></para><para>h <preformatted>
 portBASE_TYPE xQueueSendFromISR(
									 xQueueHandle pxQueue,
									 const void *pvItemToQueue,
									 portBASE_TYPE *pxHigherPriorityTaskWoken
								);
 </preformatted></para><para>This is a macro that calls xQueueGenericSendFromISR(). It is included for backward compatibility with versions of FreeRTOS.org that did not include the xQueueSendToBackFromISR() and xQueueSendToFrontFromISR() macros.</para><para>Post an item to the back of a queue. It is safe to use this function from within an interrupt service routine.</para><para>Items are queued by copy not reference so it is preferable to only queue small items, especially when called from an ISR. In most cases it would be preferable to store a pointer to the item being queued.</para><para><parameterlist kind="param"><parameteritem>
<parameternamelist>
<parametername>xQueue</parametername>
</parameternamelist>
<parameterdescription>
<para>The handle to the queue on which the item is to be posted.</para></parameterdescription>
</parameteritem>
<parameteritem>
<parameternamelist>
<parametername>pvItemToQueue</parametername>
</parameternamelist>
<parameterdescription>
<para>A pointer to the item that is to be placed on the queue. The size of the items the queue will hold was defined when the queue was created, so this many bytes will be copied from pvItemToQueue into the queue storage area.</para></parameterdescription>
</parameteritem>
<parameteritem>
<parameternamelist>
<parametername>pxHigherPriorityTaskWoken</parametername>
</parameternamelist>
<parameterdescription>
<para>xQueueSendFromISR() will set *pxHigherPriorityTaskWoken to pdTRUE if sending to the queue caused a task to unblock, and the unblocked task has a priority higher than the currently running task. If xQueueSendFromISR() sets this value to pdTRUE then a context switch should be requested before the interrupt is exited.</para></parameterdescription>
</parameteritem>
</parameterlist>
<simplesect kind="return"><para>pdTRUE if the data was successfully sent to the queue, otherwise errQUEUE_FULL.</para></simplesect>
Example usage for buffered IO (where the ISR can obtain more than one value per call): <preformatted>
 void vBufferISR( void )
 {
 char cIn;
 portBASE_TYPE xHigherPriorityTaskWoken;</preformatted></para><para><preformatted>We have not woken a task at the start of the ISR.
	xHigherPriorityTaskWoken = pdFALSE;</preformatted></para><para><preformatted>Loop until the buffer is empty.
	do
	{
Obtain a byte from the buffer.
		cIn = portINPUT_BYTE( RX_REGISTER_ADDRESS );</preformatted></para><para><preformatted>Post the byte.
		xQueueSendFromISR( xRxQueue, &amp;cIn, &amp;xHigherPriorityTaskWoken );</preformatted></para><para><preformatted>	} while( portINPUT_BYTE( BUFFER_COUNT ) );</preformatted></para><para><preformatted>Now the buffer is empty we can switch context if necessary.
	if( xHigherPriorityTaskWoken )
	{
Actual macro used here is port specific.
		taskYIELD_FROM_ISR ();
	}
 }
 </preformatted></para><para>h <preformatted>
 portBASE_TYPE xQueueGenericSendFromISR(
										   xQueueHandle	pxQueue,
										   const	void	*pvItemToQueue,
										   portBASE_TYPE	*pxHigherPriorityTaskWoken,
										   portBASE_TYPE	xCopyPosition
									   );
 </preformatted></para><para>It is preferred that the macros xQueueSendFromISR(), xQueueSendToFrontFromISR() and xQueueSendToBackFromISR() be used in place of calling this function directly.</para><para>Post an item on a queue. It is safe to use this function from within an interrupt service routine.</para><para>Items are queued by copy not reference so it is preferable to only queue small items, especially when called from an ISR. In most cases it would be preferable to store a pointer to the item being queued.</para><para><parameterlist kind="param"><parameteritem>
<parameternamelist>
<parametername>xQueue</parametername>
</parameternamelist>
<parameterdescription>
<para>The handle to the queue on which the item is to be posted.</para></parameterdescription>
</parameteritem>
<parameteritem>
<parameternamelist>
<parametername>pvItemToQueue</parametername>
</parameternamelist>
<parameterdescription>
<para>A pointer to the item that is to be placed on the queue. The size of the items the queue will hold was defined when the queue was created, so this many bytes will be copied from pvItemToQueue into the queue storage area.</para></parameterdescription>
</parameteritem>
<parameteritem>
<parameternamelist>
<parametername>pxHigherPriorityTaskWoken</parametername>
</parameternamelist>
<parameterdescription>
<para>xQueueGenericSendFromISR() will set *pxHigherPriorityTaskWoken to pdTRUE if sending to the queue caused a task to unblock, and the unblocked task has a priority higher than the currently running task. If xQueueGenericSendFromISR() sets this value to pdTRUE then a context switch should be requested before the interrupt is exited.</para></parameterdescription>
</parameteritem>
<parameteritem>
<parameternamelist>
<parametername>xCopyPosition</parametername>
</parameternamelist>
<parameterdescription>
<para>Can take the value queueSEND_TO_BACK to place the item at the back of the queue, or queueSEND_TO_FRONT to place the item at the front of the queue (for high priority messages).</para></parameterdescription>
</parameteritem>
</parameterlist>
<simplesect kind="return"><para>pdTRUE if the data was successfully sent to the queue, otherwise errQUEUE_FULL.</para></simplesect>
Example usage for buffered IO (where the ISR can obtain more than one value per call): <preformatted>
 void vBufferISR( void )
 {
 char cIn;
 portBASE_TYPE xHigherPriorityTaskWokenByPost;</preformatted></para><para><preformatted>We have not woken a task at the start of the ISR.
	xHigherPriorityTaskWokenByPost = pdFALSE;</preformatted></para><para><preformatted>Loop until the buffer is empty.
	do
	{
Obtain a byte from the buffer.
		cIn = portINPUT_BYTE( RX_REGISTER_ADDRESS );</preformatted></para><para><preformatted>Post each byte.
		xQueueGenericSendFromISR( xRxQueue, &amp;cIn, &amp;xHigherPriorityTaskWokenByPost, queueSEND_TO_BACK );</preformatted></para><para><preformatted>	} while( portINPUT_BYTE( BUFFER_COUNT ) );</preformatted></para><para><preformatted>Now the buffer is empty we can switch context if necessary.  Note that the
name of the yield function required is port specific.
	if( xHigherPriorityTaskWokenByPost )
	{
		taskYIELD_YIELD_FROM_ISR();
	}
 }
 </preformatted> </para>    </detaileddescription>
  </compounddef>
</doxygen>
